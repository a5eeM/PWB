"""
Compute area of a triangle
"""

# Get Input
base_length = float(input("Enter bases length: "))
height = float(input("Enter height: "))

# Compute
area = (base_length * height) / 2

# Display
print(f"Area of the triangle is {area:.02f}")
